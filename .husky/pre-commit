#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run lint-staged on staged files
npx lint-staged

# Run type checking on changed TypeScript files
# Temporarily disabled due to existing TypeScript errors in the codebase
# TODO: Re-enable after fixing all TypeScript errors
# npx turbo run typecheck --filter=[HEAD^1]