#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run lint-staged on staged files
npx lint-staged

# Run type checking on web package only (most critical)
# TODO: Extend to all packages after fixing TypeScript errors
cd apps/web && pnpm typecheck || true